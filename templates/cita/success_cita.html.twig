{% extends 'base.html.twig' %}

{% block title %}Cita Confirmada{% endblock %}

{% block body %}
<h2>Cita Confirmada Exitosamente</h2>
<p>¡Gracias por confirmar su cita! Aquí están los detalles de su cita:</p>

<div>
    <p><strong>Paciente:</strong> {{ app.user.nombre }} {{ app.user.apellidos }}</p>
    <p><strong>Especialidad:</strong> {{ especialidad.nombre }}</p>
    <p><strong>Médico:</strong> {{ medico.nombre }} {{ medico.apellidos }}</p>
    <p><strong>Fecha y hora:</strong> {{ fechaHora|date('d/m/Y H:i') }}</p>

    <a href="{{ path('app_inicio') }}" class="btn btn-primary">Volver al inicio</a>
</div>
{% endblock %}
